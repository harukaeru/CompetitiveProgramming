#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
  vector<char> *a = new char *(3);
  vector<char> *b = new char *(4);
  vector<char> *c = new char *(5);

  cin >> a >> b >> c;

  cout << "a: " << a << endl;
  cout << "b: " << b << endl;
  cout << "c: " << c << endl;
}