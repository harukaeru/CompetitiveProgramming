#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
  int y, m, d;
  char c1, c2;
  cin >> y >> c1 >> m >> c2 >> d;

  cout << "y: " << y << endl;
  cout << "m: " << m << endl;
  cout << "d: " << d << endl;
}