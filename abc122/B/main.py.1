#!/usr/bin/env python3
S = input().replace('\n', '')

maximum = 0
cache = 0

for j in range(len(S)):
    if S[j] in 'ACGT':
        cache += 1
        maximum = max(maximum, cache)
    else:
        cache = 0

print(maximum)
