#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
  int a, b, c, d;
  cin >> a >> b >> c >> d;

  cout << a * d - b * c << endl;
  return 0;
}
