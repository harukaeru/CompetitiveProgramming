#!/usr/bin/env python3
A, B, K = map(int, input().split())

count = 0
for i in range(min(A, B) + 1, 0, -1):
    if A % i == 0 and B % i == 0:
        count += 1
        if count == K:
            print(i)
            exit()
