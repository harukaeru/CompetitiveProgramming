#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
  string S;
  cin >> S;
  cout << S.at(1) << S.at(2) << S.at(0) << endl;

  return 0;
}
