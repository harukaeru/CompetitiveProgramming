#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

using namespace std;

int main() {
  char a;
  cin >> a;

  if (islower(a)) {
    cout << 'a' << endl;
  } else {
    cout << 'A' << endl;
  }
  return 0;
}
